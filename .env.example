# Local: core en docker/uvicorn (puerto 8090).
CORE_API_URL=http://localhost:8090

# Producción (Vercel): NO commitear .env — configurar en Vercel → Environment Variables:
# CORE_API_URL=https://api.referidosops.com  (sin barra final)
# Ver docs/conexion-core-produccion.md
